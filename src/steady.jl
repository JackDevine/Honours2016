# Functions that solve the ODE that represents the steady state.
using ODE

function steady_state(potential::Function, alpha::Number, beta::Number,
    energy::Number)
    # Find the steady state for a given potential and parameters.
    
end
